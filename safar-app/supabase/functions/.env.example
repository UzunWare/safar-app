# Supabase Edge Function Secrets
# Set these via: supabase secrets set KEY=value
# Or in Supabase Dashboard > Project Settings > Edge Functions > Secrets

# Required for process-data-export and process-data-deletion
CRON_SECRET=your_cron_secret_here

# Required for GDPR email notifications (Resend)
# Get your API key at https://resend.com/api-keys
RESEND_API_KEY=re_your_api_key_here

# Verified sender email
# Testing (no custom domain): use "onboarding@resend.dev" — only delivers to your Resend signup email
# Production (custom domain): use "Safar App <noreply@yourdomain.com>" after verifying at https://resend.com/domains
RESEND_FROM_EMAIL=onboarding@resend.dev
